/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {MockPages} from './mock-pages';

describe('MockPages', () => {
  it('should create an instance', () => {
    expect(new MockPages()).toBeTruthy();
  });
});
